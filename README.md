# Telecom Churn Prediction App

A **Machine Learning project** that predicts if a customer will churn (leave the service) based on their details.  
Built with **Python**, **XGBoost**, and **Streamlit**.

---

## 🗂 Project Structure
churn-project/
├── data/ # Dataset (CSV) - ignored on GitHub (add your own locally)
├── models/ # Trained model (.pkl) - ignored on GitHub (add your own locally)
├── notebooks/ # Jupyter notebooks
│ └── .gitkeep # placeholder file
├── screenshots/ # Screenshots of app
│ └── .gitkeep # placeholder file
├── src/ # Python scripts
│ ├── app.py
│ ├── train_model.py
│ └── predict_demo.py
├── .gitignore
├── README.md
└── requirements.txt
> Folders `data/` and `models/` are ignored in GitHub due to size, but you can add your local dataset and trained models there.

---

## 📥 Installation

1. Clone the repository:

```bash
git clone https://github.com/Vaishaliii17/churn-prediction.git
cd churn-project